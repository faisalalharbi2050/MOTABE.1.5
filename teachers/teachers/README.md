# صفحة المعلمين والإداريين - Teachers & Administrators Management

نظام متكامل لإدارة بيانات المعلمين والإداريين مع إمكانيات طباعة متقدمة وتصفية بحث شاملة.

## 📋 محتويات الحزمة

```
export/teachers/
├── app/
│   └── teachers/
│       ├── page.tsx           # الصفحة الرئيسية للمعلمين والإداريين
│       └── loading.tsx        # هيكل التحميل (skeleton)
├── components/
│   └── ui/
│       ├── Button.tsx         # مكون الزر المخصص
│       ├── Card.tsx           # مكون البطاقة
│       ├── Badge.tsx          # مكون الشارة
│       └── PageHeader.tsx     # رأس الصفحة
├── lib/
│   └── excel.ts              # دالة استيراد ملفات Excel
├── styles/
│   └── globals.css           # أنماط CSS عامة
├── tailwind.config.ts        # إعدادات Tailwind CSS
├── package.json              # اعتمادات المشروع
└── README.md                 # هذا الملف
```

## 🚀 خطوات التكامل مع مشروع Next.js جديد

### الخطوة 1: نسخ الملفات إلى مشروعك

```bash
# انسخ الملفات التالية إلى مشروعك:

# المكونات
cp -r components/ui/* /path/to/your-project/components/ui/

# مكتبات الخدمات
cp -r lib/excel.ts /path/to/your-project/lib/

# الصفحة والـ loading
cp -r app/teachers/* /path/to/your-project/app/teachers/

# الأنماط (اختياري - إذا لم تكن موجودة)
cp styles/globals.css /path/to/your-project/app/
```

### الخطوة 2: تثبيت الحزم المطلوبة

```bash
npm install react react-dom next lucide-react xlsx
npm install -D tailwindcss autoprefixer postcss typescript
```

### الخطوة 3: تحديث ملف `tailwind.config.ts` (إذا لم يكن موجوداً)

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: "#655ac1",
          light: "#8779fb",
          subtle: "#e5e1fe",
        },
        secondary: "#8779fb",
        accent: "#e5e1fe",
      },
      fontFamily: {
        sans: ['var(--font-cairo)', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
export default config;
```

### الخطوة 4: إنشاء ملف `postcss.config.js` (إذا لم يكن موجوداً)

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

### الخطوة 5: تحديث ملف `app/globals.css`

يمكنك استخدام ملف `styles/globals.css` من الحزمة أو دمج محتوياته مع ملفك الحالي.

### الخطوة 6: إضافة Route إلى `app/layout.tsx` (إذا لزم)

تأكد من أن لديك Route محدد لـ `/teachers` أو اضبط المسار الذي تفضله.

## 📋 المميزات الرئيسية

✅ **إدارة المعلمين والإداريين**
- إضافة معلم/إداري جديد
- تعديل البيانات
- حذف سجلات
- البحث والتصفية المتقدمة

✅ **نظام التخصصات الذكي**
- 22 تخصص مدرج
- تطبيع تلقائي للتخصصات المشابهة
- إعادة ترتيب التخصصات بسهولة

✅ **استيراد ملفات Excel**
- دعم صيغ `.xlsx` و `.xls`
- كشف تلقائي للأعمدة
- التحقق من التكرار

✅ **الطباعة والتقارير**
- طباعة الكل
- طباعة المعلمين فقط
- طباعة الإداريين فقط
- طباعة حسب التخصص
- تنسيق احترافي للطباعة

✅ **واجهة سهلة الاستخدام**
- تصميم Responsive
- دعم الأجهزة المحمولة (عروض Card)
- عرض الجداول على سطح المكتب
- تحميل سلس (Loading Skeleton)

✅ **تخزين البيانات**
- حفظ تلقائي في localStorage
- حفظ ترتيب التخصصات

## 🛠️ كيفية الاستخدام

### إضافة معلم جديد
1. اضغط على زر "إضافة معلم"
2. ملء البيانات (الاسم، الجوال، التخصص)
3. اختيار نصاب الحصص والانتظار
4. اضغط "حفظ"

### استيراد ملف Excel
1. اضغط على زر "استيراد"
2. اختر ملف Excel الخاص بك
3. يجب أن يحتوي الملف على الأعمدة:
   - `الاسم` أو `Name` أو `اسم المعلم`
   - `التخصص` أو `Specialization`
   - `الجوال` أو `Mobile` أو `رقم الجوال`

### الطباعة
1. اضغط على زر "طباعة"
2. اختر نوع التقرير المطلوب
3. استخدم `Ctrl+P` أو اختر الطابعة

### التعديل الجماعي
1. اضغط على زر "تعديل"
2. عدّل البيانات مباشرة في الجداول
3. اضغط "حفظ التعديلات"

## 🎨 تخصيص الألوان

يمكنك تغيير الألوان الأساسية في `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    DEFAULT: "#655ac1",  // اللون الأساسي
    light: "#8779fb",    // نسخة فاتحة
    subtle: "#e5e1fe",   // نسخة خفيفة جداً
  },
}
```

## 📱 المسارات النسبية

الملفات مستخدمة مسارات نسبية بدلاً من alias (`@/`):

```typescript
// بدلاً من:
import { Button } from "@/components/ui/Button";

// استخدم:
import { Button } from "../../components/ui/Button";
```

إذا كنت تريد استخدام alias، حدث `tsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

## 🔙 الحفظ والبيانات

- جميع البيانات تُحفظ في `localStorage` كـ:
  - `injazi_teachers`: قائمة المعلمين والإداريين
  - `injazi_spec_order`: ترتيب التخصصات المخصص
- يمكنك تغيير مفاتيح التخزين في `page.tsx`

## ⚠️ ملاحظات مهمة

1. **عدم المتوافقة مع `localStorage` في الخادم**: استخدم `"use client"` (مطبقة بالفعل)
2. **الخطوط**: تأكد من تثبيت خط Cairo (يمكنك إضافته عبر Google Fonts)
3. **الاعتمادات**: تأكد من تثبيت جميع الحزم المذكورة

## 🐛 استكشاف الأخطاء

### الصفحة لا تعرض البيانات
- تأكد من إضافة "use client" في الملف
- تحقق من console للأخطاء

### الأسلوب لا يتطبق
- تأكد من استيراد `globals.css`
- تحقق من إعدادات Tailwind

### الاستيراد من Excel لا يعمل
- تأكد من تثبيت حزمة `xlsx`
- تحقق من أسماء الأعمدة في الملف

## 📞 الدعم والتطوير

يمكنك تطوير الكود بناءً على احتياجاتك:

- إضافة قاعدة بيانات بدلاً من localStorage
- تصدير البيانات إلى PDF
- إضافة المزيد من التخصصات
- تطبيق صلاحيات المستخدمين

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم إنشاء هذا النظام بعناية لتسهيل إدارة المدارس والمؤسسات التعليمية** ✨
